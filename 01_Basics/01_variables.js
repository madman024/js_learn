const pie=3.1415
console.log(pie);

pie=4563762
console.log(pie);

/* 1)const keyword is used for declaring a const 
    constant cannot be modified
   2)var keyword was used earlier but it has scope problem so we use let 
   3)we can also not declare the type but not recommende  

*/